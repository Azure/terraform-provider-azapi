trigger:
  batch: true
  branches:
    include:
      - 'with-acctests/**'
      - 'dependabot/**'

pr: none

jobs:
  - template: ./ci.yml
    parameters:
      maxParallel: 1
      accTest: true
      vmImages:
        - ymlSafeName: ubuntu_24_04
